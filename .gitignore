# Secrets — NEVER commit
.env
*.env
!env_example.txt

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# config/experiment.yaml is the pre-registered CONTRACT — version-controlled and
# frozen. Secrets never go here; they live in .env (git-ignored above).

# Data artifacts (re-ingestable; not source)
data/store/
*.parquet
*.duckdb
*.sqlite
*.db

# Reports / plots
reports/
*.png
!docs/**/*.png

# OS / editor
.DS_Store
.idea/
.vscode/
